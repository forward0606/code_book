stringstream ss;  
ss.str("");  
ss.clear();  
getline(cin, s);  
ss<<s;
ss>>(string); // stoi can only use for 214748367 as max value.
ss>>(long long int );  // if use ss>>(long long int), it will be really strong.
                    


  
